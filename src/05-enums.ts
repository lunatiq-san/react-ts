/*
 * Enum (enumeration)
 *  - Enumeration pizza size
 *  - Default values
 *  - Custom values
 */

enum PizzaSize {
  Small = "s",
  Medium = "m",
  Large = "l",
}

console.log(PizzaSize.Small);

export {};
